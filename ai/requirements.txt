# ── Core API ──────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.27.0
pydantic>=2.7.1

# ── Local Vector Database (Replaces Qdrant) ───────
lancedb
pyarrow

# ── AI Model for Ingestion (Replaces BGE-M3) ──────
sentence-transformers

# ── Local Dashboard (Optional but recommended) ────
streamlit
pandas